# ========================================
# RAILWAY DATABASE CONFIGURATION
# ========================================
# Lấy thông tin từ Railway Dashboard > MySQL > Variables
# Tất cả team members phải dùng CÙNG credentials này

DB_HOST=your-railway-host.railway.app
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your-railway-password
DB_NAME=railway

# ========================================
# SERVER CONFIGURATION (Local)
# ========================================
PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:5173

# ========================================
# JWT SECRET
# ========================================
# QUAN TRỌNG: Tất cả team members phải dùng CÙNG JWT_SECRET
# để token authentication hoạt động đúng
JWT_SECRET=bloghub-team-secret-key-2025
JWT_EXPIRES_IN=7d

# ========================================
# HƯỚNG DẪN SETUP
# ========================================
# 1. Copy file này thành .env (trong folder server)
# 2. Thay thế các giá trị your-* bằng credentials thực từ Team Lead
# 3. Đảm bảo JWT_SECRET giống với tất cả team members
# 4. KHÔNG commit file .env lên Git
